# Virtual Clusters

Create and automatically import isolated K3s clusters inside existing Kubernetes clusters, powered by [K3k](https://github.com/rancher/k3k).

Installing this extension will automatically create new cluster and project role templates: Virtual Cluster Policy Read and Virtual Cluster Admin.